/**
 * Version of the `@strands-agents/sdk` package, as stamped from `package.json` by
 * `scripts/sync-version.mjs` (the `prebuild` npm script). Edit `package.json`, not this file.
 *
 * @internal
 */
export const SDK_VERSION = '0.0.1-development'
